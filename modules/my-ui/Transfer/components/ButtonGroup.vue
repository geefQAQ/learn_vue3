<template>
  <div class="box button-group">
    <button
      :disabled="leftButtonDisabled"
      :class="{ disabled: leftButtonDisabled}"
      @click="leftButtonClick"
    >&lt;</button>
    <button
      :disabled="rightButtonDisabled"
      :class="{ disabled: rightButtonDisabled}"
      @click="rightButtonClick"
    >&gt;</button>
  </div>
</template>

<script setup>
  const props = defineProps({
    leftButtonDisabled: {
      type: Boolean,
      default: true
    },
    rightButtonDisabled: {
      type: Boolean,
      default: true
    }
  })

  const emit = defineEmits([
    'leftButtonClick',
    'rightButtonClick'
  ])

  const leftButtonClick = () => {
    emit('leftButtonClick');
  }
  const rightButtonClick = () => {
    emit('rightButtonClick');
  }
</script>

<style lang="scss" scoped>
  .box {
    width: 120px;
    height: 100%;
  }
  .button-group {
    display: flex;
    justify-content: center;
    align-items: center;
    border-left: 1px solid #ddd;
    border-right: 1px solid #ddd;
    button {
      border: none;
      outline: none;
      width: 38px;
      height: 38px;
      background: lightseagreen;
      color: #fff;
      border-radius: 5px;
      &.disabled {
        opacity: 0.6;
        // pointer-events: none;
        cursor: not-allowed;
      }
      &:nth-child(2) {
        margin-left: 10px;
      }
    }
  }
</style>